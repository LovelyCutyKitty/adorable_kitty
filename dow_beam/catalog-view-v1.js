(()=>{const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const norm=s=>s.replace(/\*/g,' x ').replace(/\s*[x×]\s*/gi,' x ').replace(/\s+/g,' ').trim();function group(g,k){if(g!=='부자재')return g;if(k.startsWith('튜브형'))return'B1 튜브형 본드';if(k.startsWith('카트리지형'))return'B2 카트리지형 본드';if(k==='왁스')return'W1 왁스';if(k==='카본')return'CB 카본';return'MB 마보'}function kind(k){return k.replace('튜브형 본드 - ','').replace('카트리지형 본드 - ','').replace('MABOR','마보')}function draw(){if(!window.__DOW_MASTER__||!window.master)return;const m={};window.__DOW_MASTER__.forEach(([g,k,s])=>{g=group(g,k);k=kind(k);((m[g]??={})[k]??=[]).push(norm(s))});master=()=>Object.entries(m).sort((a,b)=>a[0].localeCompare(b[0],'ko',{numeric:true})).map(([g,ks])=>`<details class="stock-group"><summary>${esc(g)} <span class="order-meta">${Object.values(ks).flat().length}개 제품</span></summary>${Object.entries(ks).sort((a,b)=>a[0].localeCompare(b[0],'ko',{numeric:true})).map(([k,ss])=>`<details><summary class="master-kind34">${esc(k)} <span class="order-meta">${[...new Set(ss)].length}개</span></summary>${[...new Set(ss)].sort((a,b)=>a.localeCompare(b,'ko',{numeric:true})).map(s=>`<div class="catalog-spec">${esc(s)}</div>`).join('')}</details>`).join('')}</details>`).join('');render()};setTimeout(draw,7000)})();
